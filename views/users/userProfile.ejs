<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background-color: #f4f6f9;
        }
        .profile-container {
            max-width: 800px;
            margin: 0 auto;
            padding: 30px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }
        .profile-image {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            object-fit: cover;
            border: 4px solid #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .profile-header {
            text-align: center;
            margin-bottom: 30px;
        }
        .profile-details {
            margin-top: 20px;
            font-size: 16px;
        }
        .profile-details .row {
            margin-bottom: 15px;
        }
        .profile-details .label {
            font-weight: bold;
            color: #555;
        }
        .profile-details .value {
            color: #777;
        }
        .btn-container {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-top: 30px;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <div class="profile-container">
            <div class="profile-header">
                <!-- Profile Image -->
                <% if (user.image) { %>
                    <img src="/user_images/<%= user.image %>" alt="Profile Image" class="profile-image">
                <% } else { %>
                    <img src="https://via.placeholder.com/120" alt="Profile Image" class="profile-image">
                <% } %>
                <h3 class="mt-3"><%= user.name %></h3>
            </div>

            <div class="profile-details">
                <!-- User Details -->
                <div class="row">
                    <div class="col-4 label">Email:</div>
                    <div class="col-8 value"><%= user.email %></div>
                </div>
                <div class="row">
                    <div class="col-4 label">Phone:</div>
                    <div class="col-8 value"><%= user.phone %></div>
                </div>
                <div class="row">
                    <div class="col-4 label">Account Verified:</div>
                    <div class="col-8 value"><%= user.is_verified === 1 ? 'Yes' : 'No' %></div>
                </div>
            </div>

            <div class="btn-container">
                <a href="/edit?id=<%= user._id %>" class="btn btn-primary">Edit Profile</a>
                <a href="/home" class="btn btn-secondary">Home</a>
            </div>
        </div>
    </div>

    <!-- Bootstrap 5 JS and dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
