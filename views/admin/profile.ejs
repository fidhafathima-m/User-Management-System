<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>admin Profile</title>
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        body {
            background-color: #f4f6f9;
        }
        /* Sidebar Styles */
    .sidebar {
      position: fixed;
      top: 0;
      left: 0;
      height: 100%;
      width: 250px;
      background-color: #343a40;
      padding-top: 50px;
    }
    .sidebar a {
      color: #fff;
      text-decoration: none;
      padding: 15px;
      display: block;
      transition: background-color 0.3s ease;
    }
    .sidebar a:hover {
      background-color: #0072ff;
    }
    .sidebar a.active {
      background-color: #0072ff;
    }
    .content {
      margin-left: 250px;
      padding: 20px;
    }
    .content h1 {
      color: #0072ff;
    }
    .card {
      border-radius: 15px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }
    .card-body {
      text-align: center;
    }
    footer {
      position: fixed;
      bottom: 0;
      left: 0;
      width: 100%;
      background-color: #343a40;
      color: #fff;
      text-align: center;
      padding: 10px;
    }
        .profile-container {
            max-width: 800px;
            align-items: center;
            justify-content: center;
            margin: 0 auto;
            padding: 30px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }
        .profile-image {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            object-fit: cover;
            border: 4px solid #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .profile-header {
            text-align: center;
            margin-bottom: 30px;
        }
        .profile-details {
            margin-top: 20px;
            font-size: 16px;
        }
        .profile-details .row {
            margin-bottom: 15px;
        }
        .profile-details .label {
            font-weight: bold;
            color: #555;
        }
        .profile-details .value {
            color: #777;
        }
        .btn-container {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-top: 30px;
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <div class="sidebar">
        <h3 class="text-white text-center mb-4">Admin Panel</h3>
        <a href="/admin/home">Dashboard</a>
        <a href="/admin/usersManage">Manage Users</a>
        <a href="#" class="active">Your Profile</a>
        <a href="#">Analytics</a>
        <a href="/admin/logout">Logout</a>
    </div>
    <div class="container mt-5">
        <div class="profile-container">
            <div class="profile-header">
                <!-- Profile Image -->
                <% if (admin.image) { %>
                    <img src="/user_images/<%= admin.image %>" alt="Profile Image" class="profile-image">
                <% } else { %>
                    <img src="https://via.placeholder.com/120" alt="Profile Image" class="profile-image">
                <% } %>
                <h3 class="mt-3"><%= admin.name %></h3>
            </div>

            <div class="profile-details">
                <!-- admin Details -->
                <div class="row">
                    <div class="col-4 label">Email:</div>
                    <div class="col-8 value"><%= admin.email %></div>
                </div>
                <div class="row">
                    <div class="col-4 label">Phone:</div>
                    <div class="col-8 value"><%= admin.phone %></div>
                </div>
                <div class="row">
                    <div class="col-4 label">Account Verified:</div>
                    <div class="col-8 value"><%= admin.is_verified === 1 ? 'Yes' : 'No' %></div>
                </div>
            </div>

            <div class="btn-container">
                <a href="/admin/home" class="btn btn-secondary">Home</a>
            </div>
        </div>
    </div>

        <!-- Footer -->
    <footer>
        <p>&copy; 2024 Admin Dashboard | All Rights Reserved</p>
    </footer>

    <!-- Bootstrap 5 JS and dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
